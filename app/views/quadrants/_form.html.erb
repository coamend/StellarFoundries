<%= form_for(@quadrant) do |f| %>
  <% if @quadrant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quadrant.errors.count, "error") %> prohibited this quadrant from being saved:</h2>

      <ul>
      <% @quadrant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :universe_id %><br>
    <%= f.number_field :universe_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :quadrant_x %><br>
    <%= f.number_field :quadrant_x %>
  </div>
  <div class="field">
    <%= f.label :quadrant_y %><br>
    <%= f.number_field :quadrant_y %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

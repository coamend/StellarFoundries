<%= form_for(@planet_type) do |f| %>
  <% if @planet_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@planet_type.errors.count, "error") %> prohibited this planet_type from being saved:</h2>

      <ul>
      <% @planet_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
